---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud-redis
  namespace: nextcloud
spec:
  ports:
    - port: 6379
      targetPort: redis
  selector:
    app: nextcloud
    component: redis
  type: ClusterIP
