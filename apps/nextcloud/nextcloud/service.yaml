---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  ports:
    - port: 80
      targetPort: http
  selector:
    app: nextcloud
    component: fpm-nginx
  type: ClusterIP
