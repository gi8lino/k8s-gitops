---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: cron-egress-nextcloud
  namespace: nextcloud
spec:
  endpointSelector:
    matchLabels:
      app: nextcloud
      component: cronjob
  egress:
    - toServices:
        - k8sServiceSelector:
            selector:
              matchLabels:
                app: nextcloud
                component: fpm-nginx
            namespace: nextcloud
      toPorts:
        - ports:
            - port: "80"
