---
apiVersion: v1
kind: Service
metadata:
  name: keycloak-postgres
  namespace: keycloak
spec:
  ports:
    - port: 5432
      targetPort: postgres
  selector:
    app: keycloak
    component: postgres
  type: ClusterIP
