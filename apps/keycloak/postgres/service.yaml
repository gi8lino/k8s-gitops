---
apiVersion: v1
kind: Service
metadata:
  name: keycloak-postgres
  namespace: keycloak
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app: keycloak
    component: postgres
  type: ClusterIP
