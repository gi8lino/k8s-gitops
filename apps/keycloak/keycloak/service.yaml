---
apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: keycloak
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: keycloak
    component: keycloak
  type: ClusterIP
