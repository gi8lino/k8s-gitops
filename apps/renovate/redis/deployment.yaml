---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: renovate-redis
  namespace: renovate
  labels:
    app: renovate
    component: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: renovate
      component: redis
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: renovate
        component: redis
    spec:
      containers:
        - name: renovate-redis
          image: redis:7.0.0-alpine
          env:
            - name: TZ
              value: Europe/Zurich
          ports:
            - name: redis
              containerPort: 6379
          startupProbe:
            tcpSocket:
              port: redis
            failureThreshold: 30
            periodSeconds: 2
          livenessProbe:
            tcpSocket:
              port: redis
            failureThreshold: 2
            timeoutSeconds: 10
            periodSeconds: 30
      restartPolicy: Always
      automountServiceAccountToken: false
