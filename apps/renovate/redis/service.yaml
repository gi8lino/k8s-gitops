---
apiVersion: v1
kind: Service
metadata:
  name: renovate-redis
  namespace: renovate
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: redis
  selector:
    app: renovate
    component: redis
  type: ClusterIP
