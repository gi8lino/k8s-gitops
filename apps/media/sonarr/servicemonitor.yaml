---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sonarr
  namespace: media
spec:
  endpoints:
    - interval: 3m
      path: /metrics
      targetPort: metrics
  namespaceSelector:
    matchNames:
      - media
  selector:
    matchLabels:
      app: sonarr
