---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sonarr
  namespace: monitoring
spec:
  endpoints:
    - interval: 3m
      path: /metrics
      targetPort: metrics
  jobLabel: sonarr
  namespaceSelector:
    matchNames:
      - media
  selector:
    matchLabels:
      app: sonarr
