apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: radarr
  namespace: media
spec:
  endpointSelector:
    matchLabels:
      app: radarr
  ingress:
    - fromEndpoints:
        - matchLabels:
            app.kubernetes.io/instance: traefik
            app.kubernetes.io/name: traefik
            io.kubernetes.pod.namespace: traefik
      toPorts:
        - ports:
            - port: "7878"
  egress:
    - toEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
          rules:
            dns:
              - matchPattern: "*"
    - toFQDNs:
        - matchName: image.tmdb.org
        - matchName: radarrapi.servarr.com
      toPorts:
        - ports:
            - port: "443"
    - toServices:
        - k8sService:
            serviceName: nzbhydra2
            namespace: media
      toPorts:
        - ports:
            - port: "5076"
    - toServices:
        - k8sService:
            serviceName: plex
            namespace: media
      toPorts:
        - ports:
            - port: "32400"
    - toServices:
        - k8sService:
            serviceName: sabnzbd
            namespace: media
      toPorts:
        - ports:
            - port: "8080"
