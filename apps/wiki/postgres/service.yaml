---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: wiki
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app: wiki
    component: postgres
  type: ClusterIP
