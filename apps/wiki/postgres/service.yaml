---
apiVersion: v1
kind: Service
metadata:
  name: wiki-postgres
  namespace: wiki
spec:
  ports:
    - port: 5432
      targetPort: postgres
  selector:
    app: wiki
    component: postgres
  type: ClusterIP
