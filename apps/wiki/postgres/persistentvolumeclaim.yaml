---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wiki-postgres-data
  namespace: wiki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nvme
  resources:
    requests:
      storage: 100Mi
