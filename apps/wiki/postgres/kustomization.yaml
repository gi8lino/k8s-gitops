---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cnpg-networkpolicies
  - networkpolicies
  - cnpg-cluster.yaml
  - deployment.yaml
  - persistentvolumeclaim.yaml
  - secret.yaml
  - service.yaml
labels:
  - includeSelectors: true
    pairs:
      component: postgres
