---
apiVersion: v1
kind: Service
metadata:
  name: alerta-postgres
  namespace: alerta
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app: alerta
    component: postgres
  type: ClusterIP
