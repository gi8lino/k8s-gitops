---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alerta-postgres-data
  namespace: alerta
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nvme
  resources:
    requests:
      storage: 100Mi
