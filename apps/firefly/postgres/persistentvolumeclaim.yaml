---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefly-postgres-data
  namespace: firefly
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nvme
  resources:
    requests:
      storage: 128Mi
