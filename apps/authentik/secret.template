---
apiVersion: v1
kind: Secret
metadata:
  name: authentik-helm-values
  namespace: authentik
type: Opaque
stringData:
  values.yaml: |
    authentik:
      secret_key: # openssl rand -base64 36
      email:
        user:
        password:
        from:
      postgresql:
        password:  # if set, must be identical to the password below
    postgresql:
      postgresqlPassword:
