---
apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: filebrowser
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: filebrowser
  type: ClusterIP
