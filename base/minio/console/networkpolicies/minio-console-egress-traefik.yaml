---
apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: minio-console-egress-traefik
  namespace: traefik
spec:
  selector: app.kubernetes.io/instance == 'traefik'
  types:
    - Egress
  egress:
    - action: Allow
      protocol: TCP
      destination:
        namespaceSelector: projectcalico.org/name == 'minio'
        selector: >-
          app == 'minio' &&
          component == 'console'
        ports:
          - 9090
