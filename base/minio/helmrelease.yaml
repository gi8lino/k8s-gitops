---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: minio
  namespace: minio
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://charts.bitnami.com/bitnami
      chart: minio
      version: 9.0.1
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
      interval: 5m
  values:
    accessKey:
      password: ${MINIO_ACCESSKEY}
    extraEnv:
      - name: TZ
        value: Europe/Zurich
    ingress:
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
        traefik.ingress.kubernetes.io/router.middlewares: traefik-allow-lan-only@kubernetescrd
      enabled: true
      hostname: minio.local.${BASE_DOMAIN}
    persistence:
      storageClass: backup
    secretKey:
      password: ${MINIO_SECRETKEY}
  postRenderers:
    - kustomize:
        patchesJson6902:
          - target:
              kind: Ingress
              name: minio
            patch:
              - op: add
                path: /spec/ingressClassName
                value: traefik
          - target:
              kind: Deployment
              name: minio
            patch:
              - op: add
                path: /spec/template/spec/shareProcessNamespace
                value: true
