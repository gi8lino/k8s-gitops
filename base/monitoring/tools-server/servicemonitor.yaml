---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tools-server
  namespace: monitoring
spec:
  endpoints:
    - interval: 30s
      port: metrics
  namespaceSelector:
    matchNames:
      - monitoring
  selector:
    matchLabels:
      externalService: tools-server
