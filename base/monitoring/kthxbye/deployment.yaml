---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kthxbye
  namespace: monitoring
  labels:
    app: kthxbye
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kthxbye
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: kthxbye
    spec:
      containers:
        - name: kthxbye
          image: ghcr.io/prymitive/kthxbye:v0.15
          args:
            - -alertmanager.uri=http://alertmanager-operated.monitoring.svc.cluster.local:9093
            - -interval=60s
            - -extend-by=60m
          ports:
            - name: metrics
              containerPort: 8080
          resources:
            requests:
              memory: 50Mi
              cpu: 100m
            limits:
              memory: 150Mi
              cpu: 100m
      restartPolicy: Always
      automountServiceAccountToken: false
