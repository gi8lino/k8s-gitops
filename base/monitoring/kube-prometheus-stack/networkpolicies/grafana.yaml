---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: grafana
  namespace: monitoring
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: grafana
  ingress:
    - fromEndpoints:
        - matchLabels:
            app.kubernetes.io/instance: traefik
            app.kubernetes.io/name: traefik
            io.kubernetes.pod.namespace: traefik
        - matchLabels:
            app.kubernetes.io/instance: kube-prometheus-stack-prometheus
            app.kubernetes.io/name: prometheus
            io.kubernetes.pod.namespace: monitoring
      toPorts:
        - ports:
            - port: "3000"
  egress:
    - toEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
          rules:
            dns:
              - matchPattern: "*"
    - toEntities:
        - host
      toPorts:
        - ports:
            - port: "6443"
    - toServices:
        - k8sService:
            serviceName: kube-prometheus-stack-prometheus
            namespace: monitoring
      toPorts:
        - ports:
            - port: "9090"
    - toFQDNs:
        - matchName: grafana.net             # grafana dashboards
        - matchName: grafana.com             # grafana dashboards
        - matchName: storage.googleapis.com  # grafana plugin archive
        - matchName: sso.${BASE_DOMAIN}      # oidc: to access load balancer (https://sso.${BASE_DOMAIN})
        - matchName: raw.githubusercontent.com
      toPorts:
        - ports:
            - port: "443"
