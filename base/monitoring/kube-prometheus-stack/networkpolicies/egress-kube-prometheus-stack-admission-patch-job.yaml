---
apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: egress-kube-prometheus-stack-admission-patch-job
  namespace: monitoring
spec:
  selector: app == "kube-prometheus-stack-admission-patch"
  types:
    - Egress
  egress:
    - action: Allow
      protocol: TCP
      destination:
        nets:
          - ${HOST_IP}/32
        ports:
          - 6443
