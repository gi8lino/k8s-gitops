---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: reloader
  namespace: reloader
spec:
  interval: 5m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: reloader
      version: 1.0.69
      sourceRef:
        kind: HelmRepository
        name: stakater
        namespace: flux-system
      interval: 5m
  values:
    reloader:
      service:
        port: 9090
      serviceMonitor:
        enabled: true
