---
apiVersion: cf.containeroo.ch/v1beta1
kind: Account
metadata:
  name: main
spec:
  email: ${EMAIL}
  globalAPIKey:
    secretRef:
      name: cloudflare-api-token
      namespace: cloudflare-operator
