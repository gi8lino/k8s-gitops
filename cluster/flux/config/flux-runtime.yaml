---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-runtime
  namespace: flux-system
spec:
  dependsOn:
    - name: infra
  interval: 20m
  path: ./cluster/flux/runtime
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: cluster
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cluster-secrets
      - kind: ConfigMap
        name: cluster-settings
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
