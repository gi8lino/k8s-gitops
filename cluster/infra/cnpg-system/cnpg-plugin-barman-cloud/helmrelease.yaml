---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg-plugin-barman-cloud
  namespace: cnpg-system
spec:
  interval: 20m
  driftDetection:
    mode: enabled
  dependsOn:
    - name: cloudnative-pg
      namespace: cnpg-system
  chart:
    spec:
      chart: plugin-barman-cloud
      version: 0.5.0
      sourceRef:
        kind: HelmRepository
        name: cloudnative-pg
        namespace: flux-system
      interval: 30m
  values:
    crds:
      create: false
    certificate:
      # Renew 30 days before expiry so this internal cert clears the 21-day alert window.
      renewBefore: 720h
  install:
    crds: Skip
  upgrade:
    crds: Skip
