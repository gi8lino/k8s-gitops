---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: prometheus-egress-lore-postgres
  namespace: observability
spec:
  description: "Policy for prometheus-egress-lore-postgres"
  endpointSelector:
    matchLabels:
      app.kubernetes.io/instance: kube-prometheus-stack-prometheus
      app.kubernetes.io/name: prometheus
  egress:
    - toEndpoints:
        - matchLabels:
            cnpg.io/cluster: lore-postgres
            cnpg.io/podRole: instance
            "k8s:io.kubernetes.pod.namespace": lore
      toPorts:
        - ports:
            - port: "9187"
              protocol: TCP
