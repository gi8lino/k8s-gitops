---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: lore-postgres
  namespace: lore
spec:
  schedule: "0 0 3 * * *"
  cluster:
    name: lore-postgres
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
