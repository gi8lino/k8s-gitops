---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: motus-postgres
  namespace: motus
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:18.1
  storage:
    size: 20Gi
  bootstrap:
    initdb:
      database: motus
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: motus
  monitoring:
    enablePodMonitor: true
