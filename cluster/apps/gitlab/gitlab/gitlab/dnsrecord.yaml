---
apiVersion: cloudflare-operator.io/v1
kind: DNSRecord
metadata:
  name: gitlab-web
  namespace: gitlab
spec:
  name: git.${BASE_DOMAIN}
  type: CNAME
  content: ${BASE_DOMAIN}
  proxied: true
  ttl: 1
  interval: 5m0s
