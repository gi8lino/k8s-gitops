---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyExtensionPolicy
metadata:
  name: disable-direct-login
  namespace: kumbuka
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: kumbuka-external
  lua:
    - type: ValueRef
      valueRef:
        group: v1
        kind: ConfigMap
        name: kumbuka-external-access-controls
