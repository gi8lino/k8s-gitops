---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: minio-ingress-kumbuka-postgres
  namespace: minio
spec:
  description: "Policy for minio-ingress-kumbuka-postgres"
  endpointSelector:
    matchLabels:
      app.kubernetes.io/instance: minio
      app.kubernetes.io/name: minio
  ingress:
    - fromEndpoints:
        - matchLabels:
            cnpg.io/cluster: kumbuka-postgres
            cnpg.io/podRole: instance
            "k8s:io.kubernetes.pod.namespace": kumbuka
      toPorts:
        - ports:
            - port: "9000"
              protocol: TCP
