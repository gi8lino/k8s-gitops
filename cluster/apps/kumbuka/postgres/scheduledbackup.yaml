---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: kumbuka-postgres
  namespace: kumbuka
spec:
  schedule: "0 0 3 * * *"
  cluster:
    name: kumbuka-postgres
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
