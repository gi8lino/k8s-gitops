---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyExtensionPolicy
metadata:
  name: sabnzbd-disable-admin-page
  namespace: usenet
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: sabnzbd-external
  lua:
    - type: Inline
      inline: |
        function envoy_on_request(request_handle)
          if string.match(request_handle:headers():get(":path"), "^/config") then
            request_handle:respond(
              {
                [":status"] = "403",
              },
              "Admin page access is blocked."
            )
          end
        end
