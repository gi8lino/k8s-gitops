---
apiVersion: cloudflare-operator.io/v1
kind: DNSRecord
metadata:
  name: plex
  namespace: media
spec:
  name: plex.${BASE_DOMAIN}
  type: CNAME
  content: ${BASE_DOMAIN}
  proxied: true
  ttl: 1
  interval: 5m0s
