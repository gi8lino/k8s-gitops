---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: screendeck
  namespace: media
  annotations:
    cloudflare-operator.io/ignore: "true"
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - screendeck.${BASE_DOMAIN}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: screendeck
          port: 8080
