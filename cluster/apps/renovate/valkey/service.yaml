---
apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: renovate
spec:
  type: ClusterIP
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
  selector:
    app.kubernetes.io/name: renovate
    app.kubernetes.io/component: valkey
