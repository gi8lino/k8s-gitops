---
apiVersion: v1
kind: Service
metadata:
  name: firefly-psql-healthz
  namespace: firefly
spec:
  type: ClusterIP
  ports:
    - name: healthz
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    cnpg.io/cluster: firefly-psql
    cnpg.io/instanceRole: primary
