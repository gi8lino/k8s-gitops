---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud-valkey
  namespace: nextcloud
spec:
  interval: 20m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: valkey
      version: 0.9.3
      sourceRef:
        kind: HelmRepository
        name: valkey
        namespace: flux-system
      interval: 30m
