---
apiVersion: v1
kind: Service
metadata:
  name: traefik-forward-auth
  namespace: traefik
spec:
  ports:
    - port: 4181
      targetPort: 4181
  selector:
    app: traefik-forward-auth
  type: ClusterIP
