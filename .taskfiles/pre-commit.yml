---
version: "3"
tasks:
  init:
    desc: Initialize pre-commit hooks
    cmds:
      - pre-commit install --hook-type pre-commit
    silent: true
  run:
    desc: Run pre-commit
    cmds:
      - pre-commit run --all-files
    silent: true
