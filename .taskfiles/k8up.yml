---
version: "3"

tasks:
  c:
    desc: cleanup stucked k8up jobs in current namespace
    cmds:
      - ./hack/generate_secret_templates.sh
    silent: false

  ca:
    desc: cleanup stucked k8up jobs in all namespace
    cmds:
      - ./hack/generate_secret_templates.sh -A
    silent: false
